# JMS Serialiazer Configuration
jms_serializer:
    metadata:
        cache: file
        debug: "%kernel.debug%"
        file_cache:
            dir: "%kernel.cache_dir%/serializer"
    handlers:
        datetime:
            default_timezone: "UTC"
    property_naming:
        separator:  _
        lower_case: true