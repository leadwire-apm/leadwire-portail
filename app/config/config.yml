imports:
    - { resource: security.yml }
    - { resource: services.yml }
    - { resource: vendor/config.yml}
    - { resource: "@AppBundle/Resources/config/config.yml" }
    - { resource: "@CoreBundle/Resources/config/config.yml" }
    - { resource: "@UIBundle/Resources/config/services.yml" }
    - { resource: "@PaymentBundle/Resources/config/services.yml" }

# Parameters Configuration
parameters:
    locale: en
    app_version: 'v1.3.3'
    app_domain: '%env(APP_DOMAIN)%'
    app_schemes: '%env(APP_SCHEME)%'
    activation_code_regex: '[A-Z0-9]B[A-Z0-9]{2}7[A-Z0-9]'
    default_lock_message: 'Your account has been locked'
    secret: 06abd8f87b9d3b88feb152891421cb20017e56dd
    internal_api_prefix: api
    secret_key: '%env(STRIPE_SECRET_KEY)%'
    providerName: Stripe
    super_admin_username: '%env(SUPER_USER_ADMIN_NAME)%'
    mailer_transport: '%env(MAILER_TRANSPORT)%'
    mailer_user: '%env(MAILER_USER)%'
    mailer_password: '%env(MAILER_PASSWORD)%'
    sender: "%mailer_user%"
    github_client_id: '%env(GITHUB_CLIENT_ID)%'
    auth_providers.settings.token_secret: '%env(JWT_TOKEN_SECRET)%'
    kibana.host: '%env(KIBANA_HOST)%'
    kibana.iframe_host: '%env(KIBANA_IFRAME_HOST)%'
    kibana.port: '%env(KIBANA_PORT)%'
    kibana.iframe_port: '%env(KIBANA_IFRAME_PORT)%'
    login_method: '%env(LOGIN_METHOD)%'
    compagne_enabled: '%env(COMPAGNE_ENABLED)%'
    jenkins_url: '%env(JENKINS_URL)%'
    stripe_enabled: '%env(STRIPE_ENABLED)%'
    kibana_admin_username: '%env(KIBANA_ADMIN_USERNAME)%'
    has_all_user_tenant: false
    auth_providers:
        github:
            github_access_token_url: 'https://github.com/login/oauth/access_token'
            github_users_api_url: 'https://api.github.com/user'
            github_client_secret: '%env(GITHUB_CLIENT_SECRET)%'
        settings:
            token_secret: '%env(JWT_TOKEN_SECRET)%'
    ldap:
        host: '%env(LDAP_HOST)%'
        port: '%env(LDAP_PORT)%'
        dn_user: '%env(LDAP_DN_USER)%'
        mdp: '%env(LDAP_PASSWORD)%'
    kibana:
        host: '%env(KIBANA_HOST)%'
        port: '%env(KIBANA_PORT)%'
        iframe_host: '%env(KIBANA_IFRAME_HOST)%'
        iframe_port: '%env(KIBANA_IFRAME_PORT)%'
        kibana_admin_username: '%env(KIBANA_ADMIN_USERNAME)%'
        kibana_admin_uuid: '%env(KIBANA_ADMIN_UUID)%'
    elastic:
        username: '%env(ES_USERNAME)%'
        password: '%env(ES_PASSWORD)%'
        host: '%env(ES_HOST)%'
        port: '%env(ES_PORT)%'
    sg:
        script_path: '%env(SG_ADMIN_SCRIPT_PATH)%'
        config_dirpath: '%env(SG_ADMIN_CONF_DIR_PATH)%'
    curator:
        platformName: '%env(PLATFORM_NAME)%'
        config_path: '%env(CURATOR_CONFIG_PATH)%'
        script_path: '%env(CURATOR_SCRIPT_PATH)%'

# Framework Configuration
framework:
    translator:              { fallbacks: ['%locale%'] }
    secret:                  '%secret%'
    router:
        resource:            '%kernel.root_dir%/config/routing.yml'
        strict_requirements: ~
    validation:              { enable_annotations: true }
    templating:
        engines:             ['twig']
    default_locale:          '%locale%'
    trusted_hosts:           ~
    fragments:    ~
    http_method_override:    true

# Twig Configuration
twig:
    debug:            '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        APP_VERSION:  '%app_version%'
        BASE_URL: '%app_schemes%://%app_domain%/'
        ASSETS_BASE_URL: '/'
        UPLOAD_URL: '%app_schemes%://%app_domain%/uploads/'
        DOWNLOAD_URL: '%app_schemes%://%app_domain%/core/api/resource/'
        GITHUB_CLIENT_ID: '%github_client_id%'
        KIBANA_BASE_URL: '%kibana.iframe_host%:%kibana.iframe_port%/'
        DATE_DEFAULT_FORMAT: 'YYYY-MM-DD[T]HH:mm:ssZZ'
        EN_DATE_FORMAT: 'YYYY-DD-MM'
        FR_DATE_FORMAT: 'DD-MM-YYYY'
        TAX: 20
        LOGIN_METHOD: '%login_method%'
        COMPAGNE_ENABLED: '%compagne_enabled%'
        JENKINS_URL: '%jenkins_url%'
        STRIPE_ENABLED: '%stripe_enabled%'
    paths:
        '%kernel.project_dir%/src/UIBundle/Resources/views/Default': ~

# Assetic Configuration
assetic:
    debug:          '%kernel.debug%'
    use_controller: false
    bundles:
        - UIBundle

    filters:
        cssrewrite: ~

doctrine_mongodb:
    auto_generate_hydrator_classes: true
    default_database: '%env(DATABASE_NAME)%'
    default_connection: default
    default_document_manager: default
    connections:
        default:
            server: 'mongodb://%env(DATABASE_HOST)%:%env(DATABASE_PORT)%'
            options:
                username: ~
                password: ~
                connect: true
                db: '%env(DATABASE_NAME)%'
    document_managers:
        default:
            connection: default
            database: '%env(DATABASE_NAME)%'
            auto_mapping: true
